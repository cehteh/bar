#!/usr/bin/env bar
# -*- mode: sh; sh-shell: bash -*-
# vim: set ft=bash:
# shellcheck shell=bash

require net
require ssh

function suspend
{
    net_host_reachable "$1" && ssh_bg_cmd -o ControlMaster=no "root@$1" systemctl suspend
}

function wake
{
    net_wakonlan "$1"
}



